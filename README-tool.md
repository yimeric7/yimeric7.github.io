# Job Story Builder · Eric Yim

Static, browser-only tool. No dependencies or server. Serve this directory with any static host. Tests: `node --test logic.test.mjs` where present.

No form inputs are persisted or transmitted. No satellite-site crosslinks.
